
# inputs

product = input()
city = input()
quantity = float(input())

# variables

if city == "Sofia":
    if product == "coffee":
        price = 0.5
        total_price = quantity * price
        print(total_price)
    elif product == "water":
        price = 0.8
        total_price = quantity * price
        print(total_price)
    elif product == "beer":
        price = 1.20
        total_price = quantity * price
        print(total_price)
    elif product == "sweets":
        price = 1.45
        total_price = quantity * price
        print(total_price)
    elif product == "peanuts":
        price = 1.6
        total_price = quantity * price
        print(total_price)

elif city == "Plovdiv":
    if product == "coffee":
        price = 0.4
        total_price = quantity * price
        print(total_price)
    elif product == "water":
        price = 0.7
        total_price = quantity * price
        print(total_price)
    elif product == "beer":
        price = 1.15
        total_price = quantity * price
        print(total_price)
    elif product == "sweets":
        price = 1.30
        total_price = quantity * price
        print(total_price)
    elif product == "peanuts":
        price = 1.5
        total_price = quantity * price
        print(total_price)

elif city == "Varna":
    if product == "coffee":
        price = 0.45
        total_price = quantity * price
        print(total_price)
    elif product == "water":
        price = 0.7
        total_price = quantity * price
        print(total_price)
    elif product == "beer":
        price = 1.1
        total_price = quantity * price
        print(total_price)
    elif product == "sweets":
        price = 1.35
        total_price = quantity * price
        print(total_price)
    elif product == "peanuts":
        price = 1.55
        total_price = quantity * price
        print(total_price)


